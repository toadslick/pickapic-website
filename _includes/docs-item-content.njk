{% for item in i.items %}
  <hgroup id="{{ (i.title + " " + item.title) | slugify }}">
    <div>

      <div>
        {% for image in item.images %}
          {% set url = "/assets/help/" + image + ".l.jpg" %}
          <div>
            <a class="docs-image"  href="{{url}}" target="_blank">
              <img srcset="{{url}} 2x" width="590" height="410" loading="lazy"/>
            </a>
          </div>
        {% endfor %}
      </div>

      <h{{h}}>
        {{ item.title | safe }}
      </h{{h}}>

      {% renderTemplate "liquid, md",
      item %}
      {% for p in text %}
        {{ p }}
      {% endfor %}
      {% endrenderTemplate %}

    </div>

    {% renderFile "_includes/docs-item-content.njk", { i: item, h: h + 1 } %}

  </hgroup>
{% endfor %}